---
"bitbucket-datacenter-mcp": minor
---

Add comprehensive Bitbucket Track B tools: HTTP access token (PAT) management; SSH and GPG key lifecycle; project and repository permission controls; repository settings (default branch, PR settings, hooks); and branch model configuration.
