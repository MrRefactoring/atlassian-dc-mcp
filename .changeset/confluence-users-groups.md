---
"confluence-datacenter-mcp": minor
---

Add Confluence users and groups tools, previously entirely unconnected: self-service user lookup/profile/password tools, group and group-membership read tools, and system-administrator user/group lifecycle tools (create/update/delete/enable/disable users, create/delete groups, list active users).
