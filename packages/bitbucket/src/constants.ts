export const bitbucketServerInstructions = 'This server exposes a Bitbucket Data Center instance through MCP tools named `bitbucket_<verb>_<noun>` (e.g. bitbucket_get_pull_request, bitbucket_create_pull_request, bitbucket_delete_repository). Each tool carries annotations marking whether it is read-only, a write, or destructive — respect them and prefer read tools while exploring. Every call authenticates as the single configured user, so visible data and permitted actions are limited to that account. Project keys are upper-cased automatically. Some list tools accept `fetchAll: true` to follow pagination and return every page as a flat array. Addressable resources: bitbucket://project/{key}, bitbucket://repo/{key}/{slug}, bitbucket://pr/{key}/{slug}/{id}, bitbucket://commit/{key}/{slug}/{id}.';
