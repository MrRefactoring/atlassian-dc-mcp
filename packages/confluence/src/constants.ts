export const confluenceInstanceType = 'Confluence Data Center edition instance';

export const confluenceServerInstructions = 'This server exposes a Confluence Data Center instance through MCP tools named `confluence_<verb>_<noun>` (e.g. confluence_get_content, confluence_create_content, confluence_delete_content). Each tool carries annotations marking whether it is read-only, a write, or destructive — respect them and prefer read tools while exploring. Every call authenticates as the single configured user, so visible data and permitted actions are limited to that account. Content bodies use Confluence storage-format XHTML; use confluence_convert_content_body when unsure of the markup. Search content with CQL via confluence_search_content (the confluence_build_cql_query prompt helps compose queries). Many list tools accept `fetchAll: true` to follow pagination and return every page as a flat array. Addressable resources: confluence://page/{id}, confluence://space/{key}, confluence://space/{key}/content, confluence://user/{username}.';
