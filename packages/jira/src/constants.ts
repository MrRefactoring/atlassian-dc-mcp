export const jiraInstanceType = 'JIRA Data Center edition instance';

export const jiraServerInstructions = 'This server exposes a Jira Data Center instance through MCP tools named `jira_<verb>_<noun>` (e.g. jira_get_issue, jira_create_issue, jira_delete_issue). Each tool carries annotations marking whether it is read-only, a write, or destructive — respect them and prefer read tools while exploring. Every call authenticates as the single configured user, so visible data and permitted actions are limited to that account. Search issues with JQL via jira_search_issues; the jira_build_jql prompt helps compose queries. Many list tools accept `fetchAll: true` to follow pagination and return every page as a flat array. Addressable resources: jira://issue/{key}, jira://project/{key}, jira://board/{id}, jira://user/{username}.';
